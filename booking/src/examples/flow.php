<?

    $temp = rand(-50,100);

    // estimates
    $estimation = $temp >= 18 ? "Warm" : "Cold";

    /* if( $temp >= 18) {
        $estimation = "Warm";
    } else {
        $estimation = "Cold";
    } */

    // send data
    print($estimation);






